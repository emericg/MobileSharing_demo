<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto"
          android:versionName="0.1" android:versionCode="000101">

    <supports-screens android:anyDensity="true" android:largeScreens="true" android:normalScreens="true" android:smallScreens="true" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- MobileSharing: (OPTIONAL / used by checkMimeTypeView())
         declare the ACTION_VIEW intent we resolve, so Android 11+ package visibility doesn't hide handlers from resolveActivity() -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <!-- Application -->
    <application android:name="org.qtproject.qt.android.bindings.QtApplication"
                 android:label="MobileSharing">

        <!-- MobileSharing: use the module's QShareActivity (OPTIONAL / handles incoming SEND/VIEW intents) -->
        <activity android:name="io.emeric.mobilesharing.QShareActivity"
                  android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
                  android:launchMode="singleTask" android:screenOrientation="unspecified" android:exported="true"
                  android:windowSoftInputMode="adjustPan"
                  android:label="MobileSharing">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- MobileSharing: OPTIONAL / Handle incoming content shared into this app 
                 Use either or both SEND / SEND_MULTIPLE, and customize accepted incoming mime types -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="image/*" />
                <data android:scheme="file" />
                <data android:scheme="content" />
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="-- %%INSERT_APP_LIB_NAME%% --" />
            <meta-data android:name="android.app.arguments" android:value="-- %%INSERT_APP_ARGUMENTS%% --" />
            <meta-data android:name="android.app.extract_android_style" android:value="minimal" />
            <meta-data android:name="android.app.background_running" android:value="false" />
        </activity>

        <!-- MobileSharing: OPTIONAL / Handle outgoing content -->
        <provider android:name="androidx.core.content.FileProvider"
                  android:authorities="${applicationId}.fileprovider"
                  android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                       android:resource="@xml/filepaths" />
        </provider>

        <!-- MobileSharing: (OPTIONAL / DocumentsProvider support (Storage Access Framework))
             A DocumentsProvider can expose an app-owned directory to other apps through the system file picker.
             It is declared here but stays inert until enabled at runtime via MobileSharing.setDocumentProviderEnabled(true).
             The MANAGE_DOCUMENTS permission means only the system DocumentsUI can bind to it. -->
        <provider android:name="io.emeric.mobilesharing.QShareDocumentProvider"
                  android:authorities="${applicationId}.documents"
                  android:exported="true" android:grantUriPermissions="true"
                  android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

    </application>

</manifest>
